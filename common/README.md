# Shared Code

Reusable code that supports more than one course project belongs here.

- `optimization/`: optimization algorithms, line searches, constraints, and numerical checks.
- `visualization/`: common plotting styles and figure-export helpers.
- `utils/`: configuration, reproducibility, and general utilities.

Project-specific experiments should remain inside the corresponding `projects/<project>/src/` directory until they are genuinely reusable.
